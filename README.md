# Hosting-a-Website-on-AWS-EC2-Ubuntu-
Hosting a website on AWS EC2 involves launching an Ubuntu instance, connecting via SSH using a key pair, and installing the Apache web server. Port 80 is enabled in the security group, and a custom HTML file is added to /var/www/html. The site is accessed using the instance’s public IP.
